fastapi>=0.138.1,<1.0
uvicorn[standard]>=0.49.0
sqlalchemy>=2.0,<2.1
# DB schema migrations (versioned create/alter for SQLite + Postgres).
alembic>=1.18.5
pydantic>=2.13.4
pydantic-settings>=2.3
httpx>=0.28.1
# Postgres driver (used in Docker/PaaS; local dev uses SQLite and won't import it)
psycopg2-binary>=2.9
# Bundled IANA tz data so zoneinfo("Europe/Berlin") works on slim images (Render) that
# strip the system tzdb — keeps per-offer validity dates host-independent.
tzdata>=2024.1
# Error tracking (opt-in): only initialises when SENTRY_DSN is set. The [fastapi] extra
# pulls the Starlette/FastAPI integration so unhandled 500s are captured automatically.
sentry-sdk[fastapi]>=2.63.0
# Dev / testing (also used by CI: pytest + ruff lint + coverage)
pytest>=9.1.1
pytest-cov>=7.1.0
ruff>=0.15
# Property-based testing for the parsers that eat external junk (Grundpreis strings,
# discount labels, validity timestamps). CI runs it derandomized (see tests/conftest.py),
# so it can never flake there; randomness only hunts locally.
hypothesis>=6.100
